name: Build & Test

on: [push]
jobs:
  build-test-linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: recursive
    - name: Setup cmake
      uses: jwlawson/actions-setup-cmake@v1.9
      with:
        cmake-version: '3.20.x'
    - name: Set up GCC
      uses: egor-tensin/setup-gcc@v1
      with:
        version: 11
        platform: x64
    - name: Install Qt
      uses: jurplel/install-qt-action@v2
      with:
        version: '6.2.1'
        setup-python: false
    
    - name: Install Curl
      run: sudo apt install -y sudo curl libssl-dev libcurl4-openssl-dev

    - name: Build ROSPackageManager
      env:
        CC: gcc-11
        CXX: g++-11 
      run: |
            cmake -Bbuild
            cmake --build build
    - uses: actions/upload-artifact@v1
      with:
          name: ubuntu_rospkg
          path: build/ROSPkgManager
  build-test-macos:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: recursive
  
    - name: Setup cmake
      uses: jwlawson/actions-setup-cmake@v1.9
      with:
        cmake-version: '3.20.x'

    - name: Install Curl
      run: brew install curl

    - name: Install Qt
      uses: jurplel/install-qt-action@v2
      with:
        version: '6.2.1'
        setup-python: false
    - name: Build ROSPackageManager
      run: |
            cmake -Bbuild
            cmake --build build
    - uses: actions/upload-artifact@v1
      with:
          name: darwin_rospkg
          path: build/ROSPkgManager.app
  build-test-windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: recursive

    - name: Install ZLIB
      run: powershell -Command "(Invoke-WebRequest -Uri https://git.io/JnHTY -OutFile install_zlib.bat)"; ./install_zlib.bat; del install_zlib.bat
    
    - uses: msys2/setup-msys2@v2
      with:
        msystem: mingw64
        update: true
        install: >-
          cmake
          mingw-w64-x86_64-openssl
          gcc
    
    - name: Install Qt
      uses: jurplel/install-qt-action@v2
      with:
        version: '6.2.1'
        arch: 'win64_mingw81'

    - name: GCC version
      env:
        MINGW_HOME: C:\msys64\mingw64
        MSYS_HOME: C:\msys64
      run: echo "PATH=${MSYS_HOME}\usr\bin;${MINGW_HOME}\bin:$PATH" >> $GITHUB_ENV
      shell: msys2 {0}
  
  
    - name: Build ROSPackageManager 
      run: |
            cmake -Bbuild -G "Unix Makefiles"
            cmake --build build --config Release 
      shell: msys2 {0}
      env:
        CC: gcc
        CXX: g++
  
    - uses: actions/upload-artifact@v1
      with:
          name: win_rospkg
          path: build/ROSPkgManager.exe
